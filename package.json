{
  "name": "pomo-focus-timer",
  "version": "1.0.0",
  "description": "Pomo — A beautiful Pomodoro focus timer",
  "private": true,
  "scripts": {
    "build:ios":     "npx cap add ios && npx cap sync ios && npx cap open ios",
    "build:android": "npx cap add android && npx cap sync android && npx cap open android",
    "sync":          "npx cap sync",
    "open:ios":      "npx cap open ios",
    "open:android":  "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0",
    "@capacitor/local-notifications": "^5.0.0",
    "@capacitor/haptics": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
